#include <stdio.h>
#include <stdlib.h>
#include "validator.h"

void clearBuffer(void)
{
    char memoryBuffer = '\n';    
    while (getchar() != memoryBuffer)
    {
        /**< Without implementation to clean memory */
    }
}
